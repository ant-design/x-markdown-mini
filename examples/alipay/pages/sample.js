// 此文件由 examples/sample.js 生成，请勿直接修改。
// 修改共享内容后运行：npm run sync:examples
const SAMPLE = "# x-markdown-mini\n\n这是一段刻意写得稍长的示例，用来观察 Markdown 在小程序里的流式解析、逐字出现和稳定块提交。打开右上角的「打字机效果」后，内容会像真实 AI 回复一样分批到达；段落、列表、代码和公式应当自然接续，已经完成的部分不应反复闪烁或跳动。\n\n渲染器同时支持 **粗体**、*斜体*、~~删除线~~、`行内代码` 和 [链接](https://github.com/ant-design/x-markdown-mini)。行内元素会随着文字一起出现，而表格、代码块和数学公式会在语法完整后成为稳定内容。\n\n## 一次流式回复会经历什么\n\n1. 接收增量字符，并保留截至当前的完整 Markdown。\n2. 识别由双空行结束的稳定块，只重新解析仍在变化的尾部。\n3. 将 token 直接转换成目标平台节点。\n   - 微信使用 `tokensToWechat`。\n   - 支付宝使用 `tokensToAlipay`。\n4. 内容结束后关闭流式状态，页面停留在最终结果。\n\n> 观察重点：文字逐步增长时，前面的段落应保持稳定；遇到未闭合的强调、代码围栏或公式时，尾部可以被修补，但不应影响已经提交的内容。\n\n## 接入方式对照\n\n| 方案 | 接入成本 | 控制能力 | 适用场景 |\n| --- | --- | --- | --- |\n| 组件接入 | 低 | 常用属性 | 聊天消息、说明文档 |\n| JS 接入 | 中 | 实例与扩展完全可控 | 定制渲染、复杂交互 |\n\n两种页面读取的是同一份示例内容，因此可以直接比较最终节点结构和视觉结果。差异只应来自平台能力，不应来自手工维护了两份不同文案。\n\n## 代码高亮\n\n```js\nconst md = new XMarkdownMini({\n  escapeText: false,\n  extensions: [Latex(), CodeHighlight()],\n});\n\nconst nodes = md.renderNodes({ content, platform: 'alipay' });\n```\n\n代码围栏闭合前属于流式尾部；闭合后，语言标签、复制按钮和高亮内容应一次稳定下来。这个段落继续增加一些文字，让你有足够时间观察字符淡入、标点停顿以及滚动区域的增长，而不是刚打开开关就瞬间播放结束。\n\n## 数学公式\n\n公式 1 是简单的行内上标：$E = mc^2$。数字 2 应贴合在字母右上方，基线不能把整行文字向上顶。\n\n公式 2 同时包含求和上下限和分式，用来检查微信端 KaTeX 的纵向列表定位：\n\n$$\n\\sum_{i=1}^{n} i = \\frac{n(n+1)}{2}\n$$\n\n公式完成后，求和符号上方的 `n`、下方的 `i = 1`、分子和分母都应围绕各自基线排列，不能整体向上漂移。流式过程中即使公式暂时不完整，前面的文本也应保持原位。\n\n## 收尾\n\n到这里，一次较长的模拟回复已经覆盖普通段落、富文本、嵌套列表、引用、表格、代码高亮和 LaTeX。关闭打字机效果会立即展示全文，再次打开则从头播放，便于反复观察同一个问题。";

module.exports = { SAMPLE };
