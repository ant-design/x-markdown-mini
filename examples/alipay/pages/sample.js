// 两个示例页（组件接入 / JS 接入）渲染同一份内容，便于直接对照渲染结果。
// 覆盖：标题 / 强调 / 列表 / 表格 / 行内代码 / 代码高亮 / 行内 + 块级 LaTeX / 引用。
// 用数组 join 而非模板字符串：内容里含 ``` 代码围栏，避免与 JS 反引号冲突。
const SAMPLE = [
  '# x-markdown-mini',
  '',
  '**低接入成本**、高性能的小程序 Markdown 渲染器，支持 *斜体*、`行内代码`、~~删除线~~ 与 [链接](https://github.com)。',
  '',
  '## 列表',
  '',
  '1. 组件接入：一行 `usingComponents` 即可',
  '2. JS 接入：`renderNodes()` 直出节点',
  '   - 支持流式渲染',
  '   - 支持自定义组件',
  '',
  '## 表格',
  '',
  '| 方案 | 接入成本 | 适用场景 |',
  '| --- | --- | --- |',
  '| 组件 | 极低 | 直接展示 |',
  '| JS | 中 | 需要精细控制 |',
  '',
  '## 代码高亮',
  '',
  '```js',
  'const md = new XMarkdownMini();',
  "const nodes = md.renderNodes({ content, platform: 'alipay' });",
  '```',
  '',
  '## 数学公式',
  '',
  '行内公式 $E = mc^2$，以及块级公式：',
  '',
  '$$',
  '\\sum_{i=1}^{n} i = \\frac{n(n+1)}{2}',
  '$$',
  '',
  '> 同一份内容，组件接入与 JS 接入的渲染结果应当一致。',
].join('\n');

module.exports = { SAMPLE };
