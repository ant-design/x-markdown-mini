<view class="page">
  <view class="note">
    <text class="note-tag">JS 接入</text>
    页面 require 核心 + 插件，renderNodes() 直出节点，交给 MiniNodeRenderer 渲染。
    直接用底层组件时，样式需自行 @import（见 js.acss）。
  </view>

  <view class="card">
    <mini-node-renderer nodes="{{nodes}}" />
  </view>
</view>
