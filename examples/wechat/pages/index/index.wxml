<view class="page">
  <view class="header">
    <view class="title">x-markdown-mini · WeChat</view>
    <view class="actions">
      <button size="mini" type="default" bindtap="reset">一次性</button>
      <button size="mini" type="primary" bindtap="startStream">流式</button>
    </view>
  </view>

  <view class="card">
    <markdown
      content="{{content}}"
      streaming="{{streaming}}"
      bind:rendercomplete="onRenderComplete"
    />
  </view>
</view>
